"""Слой ИСПОЛНЕНИЯ. Науки здесь нет и быть не должно.

Граница проведена физически, а не комментарием: научный код живёт в
`research/python` и закреплён `SCIENCE_SHA`, этот пакет лежит отдельным
деревом и закреплён `EXECUTION_SHA`. Производственный прогон выкладывает
их РАЗНЫМИ checkout'ами, поэтому «случайно поправить науку, правя
планировщик» технически невозможно: файлы приезжают из разных коммитов.

Что здесь можно менять свободно:

    какие юниты считать дальше, как их разложить по заданиям, сколько
    стоит юнит, как называть артефакты, что писать в provenance

Что здесь нельзя ни при каких условиях:

    генератор, критерий точности, семантика концов, `s5b_shard.merge`,
    `cell_contrast` — они ИМПОРТИРУЮТСЯ из замороженного дерева и
    вызываются как есть

Правило проверяется гейтом: ни один модуль пакета не смеет ПЕРЕОПРЕДЕЛЯТЬ
научную функцию, только звать её.
"""

from __future__ import annotations

#: Научные модули, которые слой вправе только ЗВАТЬ
FROZEN_SCIENCE = ("simulation.s5b_shard", "simulation.s5b_escalation",
                  "simulation.s5b_precision", "simulation.s5b_prereg")

EXECUTION_LAYER_OWNS_NO_SCIENCE = True
