# PREREG S5b — sensitivity-calibrated design study

**Статус: заморожен до прогона, редакция после враждебного чтения.** Ни
одного запуска здесь нет. Машинные константы —
`research/python/simulation/s5b_prereg.py`, дисциплина —
`tests/test_s5b_prereg.py`. Одиннадцать поправок первого враждебного чтения
внесены и помечены в тексте как **ПОПРАВКА**; прежние формулировки оставлены
рядом, потому что стёртая ошибка перестаёт быть уроком.

## 0. Назначение и запрет назначения

S5b **не** считает «нужное число участников». Он очерчивает **область
допустимого дизайна**.

Вопрос после S4 и Lean изменился и звучит теперь так:

> при каких условиях источник данных идентифицирует causal endpoint
> достаточно хорошо, чтобы эксперимент имел смысл?

Качество acquisition стало **параметром дизайна**, а не инженерной
подробностью.

**НЕ назначение:**

```
выбрать pilot N одним числом
выбрать δ по тому, где график красивее
выбрать H по тому, где график красивее
оценить эффект пакета C на поведение
объявить откалиброванной абсолютную incidence
```

### ПОПРАВКА 1 (P0). «C сокращается в контрасте» — было слишком сильно

Прежняя редакция писала: пакет `C` стоит в обеих руках, значит в контрасте
сокращается, значит способен испортить измеримость, но не оценку. Вторая
половина этого утверждения неверна. Арифметически

```
Y(C,T) − Y(C,K)   НЕ обязано равняться   Y(0,T) − Y(0,K)
```

потому что генератор нелинеен: латентность двигает слияние серий, incidence
едет следом эмерджентно, а величина эффекта `T` зависит от рабочей точки.
Возможно взаимодействие `C × T`, и смена `C` меняет **и** измеримость, **и**
сам conditional ITT. Это не смещение — это другой эстиманд при другом общем
пакете.

Практическое следствие ровно одно и оно неприятное: если на оси C-reactivity
контраст поползёт, прежняя редакция объявила бы это «тем, чего ждать
неоткуда» — то есть приняла бы законный результат за баг.

```
C_HAS_NO_SEPARATELY_IDENTIFIABLE_EFFECT            руки без C не существует
C_MAY_ALTER_BOTH_MEASURABILITY_AND_CONDITIONAL_ITT  и то и другое законно
NEITHER_CHANGE_IS_AN_ESTIMATE_OF_C_VS_NO_C          ни одно не есть оценка C
```

## 1. Что разрешено брать из S4 — и что остаётся неизвестным

| разрешено | неизвестно |
|---|---|
| оператор огрубления `C_Δ` и его действие на топологию | абсолютный `opportunity_rate_per_day` |
| поведение грубого наблюдателя (`TiePolicy.BOUNDED`) | истинная доля неответов при полном покрытии |
| связь ширины идентификации с локальной плотностью | incidence бремени на фиксированном окне |
| `STRICT` считает другой функционал и как primary не годится | реальная величина measurement reactivity `C` |

## 2. Четыре первичных вопроса

1. **Область идентифицируемости.** Для каких сочетаний `opportunity_rate ×
   локальная структура × разрешение меток × режим эффекта × H` эндпоинт
   вообще достаточно идентифицирован, а где `MEASUREMENT_AMBIGUOUS` —
   структурный исход?
2. **Выборка против структурной неоднозначности.** Где рост числа диад сужает
   `CI` и меняет решение, а где не помогает, потому что сам интервал
   идентификации пересекает границу решения?
   ```
   больше N   лечит sampling uncertainty
   больше N   НЕ лечит потерянную хронологию
   ```
3. **Чувствительность к пакету C.** Как рабочая точка и ширина идентификации
   движутся по объявленной оси C-reactivity. **ПОПРАВКА 10 (P1):** вопрос
   больше не обещает ответа «выполним ли операционный бюджет C» — см. §7.
4. **Требования к источнику.** При каком **разрешении** источник перестаёт
   быть пригодным? Выход — не поверхность мощности, а **область допустимости
   измерения**: `usable` / `borderline` / `unusable`.
   **ПОПРАВКА 11 (P1):** прежняя формулировка обещала «разрешение И contract
   покрытия». Оси покрытия в сетке нет — `OBSERVATION_DAYS` это длина окна, а
   не полнота захвата. S4 именно на capture-window и упёрся, так и не сумев
   его закрыть; моделировать здесь липовый contract значило бы изобретать ту
   самую величину, которую не удалось измерить.
   ```
   Q4_SCOPE      temporal-resolution requirements
                 UNDER ASSUMED COMPLETE COVERAGE
   COVERAGE_AXIS NOT MODELLED
   ```

## 3. Оси чувствительности — узлы объявлены, правило названо

```
opportunity_rate  1.5 · 6.0 · 24.0 · 96.0        дефолт генератора 6.0,
                                                  множители 1/4, 1, 4, 16
разрешение, с     1 · 5 · 15 · 60                грубее 60 нет ни одного
                                                  реального кандидата
H                 5 мин · 60 мин · 24 ч          ОСЬ, не решение
δ                 1% · 3% · 10% от H             БЕЗРАЗМЕРНО, ОСЬ, не решение
режим эффекта     R0 · R1 · R2 · R3 · R4         уже объявлены в experiment
множитель эффекта 0.5 · 1.0 · 2.0                НЕ применяется к R0
диад              25 · 50 · 100 · 200 · 400
дней наблюдения   14 · 28 · 56
C-reactivity      (1.00, 0.00) · (1.10, −0.10) · (1.25, −0.25)   ПАРАМИ
```

**Узлы после первого прогона не двигаются.** «Посмотрим 1…20 и выберем
полезную часть» — это preregistration, действующее ровно до первого
неудобного результата.

`δ` задана **безразмерно**, долями горизонта, намеренно: абсолютное число
потребовало бы содержательного основания «что практически значимо», которого
у нас пока нет, а взятое с графика оно было бы выбрано по красоте картинки.

### ПОПРАВКА 7 (P1). Что значит «множитель эффекта»

Естественных смыслов минимум три, и они расходятся не на проценты. Для
`RATE_RATIO = 0.75` «половина эффекта» — это `0.875` (линейно от единицы),
`0.866` (по логарифму) или `0.375` (тупое умножение, которое делает
половинный эффект **вдвое сильнее** полного). Выбрана логарифмическая шкала:
согласована для обоих каналов, монотонна, даёт ноль при `m = 0`.

```
latency_log_shift  ->  m · shift
rate_ratio         ->  ratio ** m
R3                 ->  оба канала одним и тем же m
R4                 ->  m применяется к БАЗОВОМУ эффекту ДО расщепления
```

У `R0` множитель бессмыслен: три «разных» нуля дали бы три одинаковых
сценария и слегка соврали бы о размере сетки. `R0` считается один раз.

### ПОПРАВКА 6 (P1). C-reactivity — три сценария, а не девять

Прежняя запись двумя независимыми кортежами (`rate ×1.0/1.10/1.25`,
`shift 0/−0.10/−0.25`) допускала два одинаково разумных прочтения — три
сценария парами или девять декартовых, — а счётчик ячеек молча предполагал
первое. Мелкий баг, но образцовый: ровно за этим враждебное чтение и делается
**до** поверхности, а не после. Теперь точки записаны парами явно.

## 4. `STRICT` — только диагностический контроль

```
BOUNDED   допустимая политика измерения
STRICT    диагностический негативный контроль
```

Никаких таблиц «какой из двух лучше»: они позволили бы в удобной области
выбрать `STRICT`. После S4 его место ровно одно — показывать, как выглядит
отбор по исходу.

## 5. ПОПРАВКА 2 (P0). От интервалов к решению — цепочка прибита целиком

**Главный блокер прежней редакции.** Она называла «интервал идентификации
контраста» и «доверительный интервал», но не говорила, **как** они строятся.
Это оставляло runner'у выбор между midpoint CI, двумя независимыми CI на
концах, bootstrap и ещё четырьмя разумно звучащими вариантами — то есть
позволяло тихо принять научное решение и назвать его реализацией.

```
1  наблюдённые грубые данные
2  BOUNDED на person-period                 ->  [L_i, U_i]
3  границы руки                             ->  [mean L_i, mean U_i]
4  границы контраста                        ->  [meanL_T − meanU_K,
                                                 meanU_T − meanL_K]
5  выборочная неопределённость ОТДЕЛЬНО для каждого конца
6  консервативная внешняя оболочка          ->  решение
```

**Почему шаг 3 — именно средние концов.** Эстиманд руки —
person-period-weighted среднее. Значение каждой ячейки известно лишь с
точностью до `[L_i, U_i]`, межъячеечных ограничений нет (разные диады),
поэтому идентифицированное множество среднего есть в точности
`[mean L_i, mean U_i]`. Резко, а не консервативно.

**Почему шаг 4 — именно так.** Руки независимы, поэтому наименьшая разность
достигается при наименьшем `T` и наибольшем `K`, и наоборот. Тоже резко.

**Шаг 5–6 консервативны, и это признано вслух.** Оцениваются четыре средних;
оба конца контраста — разности независимых выборочных средних. Для каждого
конца строится двусторонний интервал уровня `0.95` по той же Стьюдент-машинерии
(Уэлч), что уже качественно проверена в S5a, а решающая оболочка берётся как

```
[ нижняя граница CI нижнего конца , верхняя граница CI верхнего конца ]
```

Она перекрывает с запасом. Imbens-Manski-подобная конструкция дала бы уже, и
она записана как `DEFERRED`, а не как «мы не подумали».

## 6. Что значит «достаточно идентифицирован» — объявлено заранее

**Два разных гейта**, и сваливать их в один `INCONCLUSIVE` запрещено:

```
measurement gate   популяционный интервал идентификации КОНТРАСТА,
                   оценённый при ОПОРНОМ N, не пересекает ±δ
sampling gate      консервативная внешняя оболочка не пересекает ±δ
```

### ПОПРАВКА 5 (P1). Гейт измерения не «не зависит от N», а меряется при опорном N

Прежняя редакция говорила, что гейт измерения «от числа диад не зависит
вовсе». Слишком сильно: реализованный на конечной выборке интервал контраста
складывается из ячеек и с составом выборки меняется. Не меняется другое —
**структурная**, то есть популяционная ширина. Её и меряем: при объявленном
опорном числе диад, отдельно от лестницы `N`.

```
MEASUREMENT_GATE_REFERENCE_DYADS        500
STRUCTURAL_AMBIGUITY_IS_NOT_CURED_BY_N  True
REALIZED_INTERVAL_DOES_DEPEND_ON_N      True
```

**Комбинированный вердикт — четыре состояния:**

| вердикт | что означает |
|---|---|
| `IDENTIFIED_AND_DECISIVE` | измерение идентифицирует, выборка решает |
| `SAMPLING_INCONCLUSIVE` | **больше N поможет** |
| `MEASUREMENT_AMBIGUOUS` | **больше N не поможет** |
| `BOTH_AMBIGUOUS` | и то и другое |

Третье и второе — не одно и то же. Одно лечится участниками, другое не
лечится ничем. Это различие куплено слишком дорого, чтобы потерять его в
дизайне мощности.

### ПОПРАВКА 8 (P1). `BORDERLINE` перестаёт быть словом и становится правилом

Оценка популяционного интервала конечна, значит у гейта нужен допуск — и он
же задаёт третью категорию. Ячейка `BORDERLINE`, если хоть один конец
интервала лежит к `±δ` ближе, чем на `GATE_MARGIN_FRACTION · δ`.

```
GATE_MARGIN_FRACTION   0.10
admissibility(low, high, delta) -> USABLE | BORDERLINE | UNUSABLE
```

Выбор «двух BORDERLINE-ячеек» для третьего этапа тоже детерминирован —
иначе при сорока семи пограничных ячейках «две» это researcher degree of
freedom, применяемый после просмотра поверхности:

```
ближайшая к гейту ИЗНУТРИ и ближайшая СНАРУЖИ,
ничьи — лексикографически по (rate, resolution, horizon, delta_fraction,
                              regime, magnitude, c_point)
```

## 6a. ПОПРАВКА 3 (P0). Правая граница окна — решена сейчас, а не отложена

Семантика `N_eligible` на правой границе была `DEFERRED` «до появления
настоящих fixed person-period окон». Они появились **прямо здесь**:
`OBSERVATION_DAYS` — это и есть фиксированные окна, а ось разрешений делает
контрпример частью исследования, а не гипотетикой:

```
t = 119, H = 60, окно до 120:   N(1 с) = 0,   N(60 с) = 1
```

Край света вошёл в сетку сам. Оставить это на потом значило бы сравнивать по
оси разрешений краевой артефакт и называть его топологией.

**Решение — вариант B: общее внутреннее окно анализа, инвариантное к
разрешению.** Eligibility вычисляется один раз при **опорном разрешении** —
самом грубом на оси, 60 с, — и одно и то же множество возможностей
используется во всех руках оси разрешений.

```
ELIGIBILITY_RULE   opening eligible iff its bin start at the reference
                   resolution satisfies start + H <= window_end;
                   the same eligible set is used for every resolution arm
ELIGIBILITY_GUARD_BINS   1
```

Почему не guard band: сдвиг границы на 60 с делает мелкое eligible
подмножеством грубого, но **не** равенством. Инвариантность даёт только
единая опорная сетка. Это дизайн-решение, делающее ось разрешений
интерпретируемой, а не открытие.

## 7. ПОПРАВКА 10 (P1). Операционный бюджет C — вынесен из S5b

Прежняя редакция обещала, что S5b ответит «выполним ли бюджет C». Технически
не может. В S5b нет и не будет модели, связывающей

```
точка C-reactivity <-> prompts <-> ручные минуты <-> повторы <-> пропуски
```

Изобретать связь между «rate ×1.25» и «1.7 prompts в сутки», которой никто не
наблюдал и не задавал, — ровно тот жанр, от которого проект отучался весь S4.
Вопрос расщеплён:

```
S5b        огибающая чувствительности к reactivity
freeze C   операционное бремя кандидата C против бюджета
```

Числа бюджета остаются записанными — `≤ 2` prompts в сутки, `≤ 10` ручных
минут в неделю, `≤ 1` повтор на период, `≤ 20%` пропущенных периодов, — но
как **позднейший гейт**, а не как выход S5b. Если тогда рабочая точка
выполнима только при их нарушении — переделывается `C`, а не критерий.

## 8. Выход — поверхность, а не число

Для каждой ячейки: минимальное `N` из объявленного набора, удовлетворяющее
критерию, **либо**

```
NO_FINITE_N_SOLVES_MEASUREMENT_AMBIGUITY
```

Эта категория — самая ценная из всех, и она **структурная**: про
популяционный интервал при опорном `N`, а не про одну конечную реализацию.
Иначе «источник не подходит» можно было бы получить невезением. Если при
грубом сборе никакой разумный `N` не помогает, диагноз не «низкая мощность»,
а **«источник не подходит»**. Очень разные диагнозы, а таблицы power любят
притворяться, что всё лечится ещё сотней участников.

## 9. ПОПРАВКА 4 (P0). Правило урезания сетки — прежнее было неверным

Прежний этап 1 считал поверхность измеримости при `R1` и по ней отсекал
лестницу `N`. Это неверно, и неверно по главному результату S4:

```
эффект  ->  плотность / топология  ->  неоднозначность
```

Измеримость зависит от поведения, а поведение — от режима эффекта. Значит
«`R1` прошёл» не означает «`R2/R3/R4` прошли», и наоборот. Отсечение было бы
великолепно оптимизированным экспериментом с неправильным правилом. Поэтому
**все** оси, способные менять топологию, входят в этап 1.

```
STAGE_ONE_AXES   opportunity_rate · resolution · horizon · delta_fraction
                 effect_regime · effect_magnitude · c_reactivity_point
```

**Честная экономия — вычислительная, а не научная.** Разрешение не требует
отдельной симуляции: все четыре руки получаются применением `C_Δ` к одному и
тому же латентному потоку. Это ещё и лучше научно — сравнение по оси
разрешений становится **парным** и не несёт монте-карловского шума между
руками. `H` и `δ` тоже не требуют симуляции: горизонт применяется к тем же
возможностям, `δ` — порог на уже посчитанном контрасте.

Длительность наблюдения в этап 1 не входит, и это обосновано, а не удобно:
единственным каналом её влияния на гейт измерения была правая граница, а она
теперь инвариантна по построению (§6a).

```
латентных симуляций этапа 1     156   = 4 rate × 13 regime×magnitude × 3 C
оценок гейта измерения          5616  = 156 × 4 resolution × 3 H × 3 δ
```

Тринадцать — это `R0` один раз плюс `R1…R4` по три множителя. Обе цифры
видны **до** прогона и заморожены тестом.

```
ЭТАП 1  поверхность измеримости по ВСЕМ осям, меняющим топологию;
        разрешение, H и δ достаются из тех же потоков бесплатно
ЭТАП 2  лестница N × длительность ТОЛЬКО там, где гейт измерения пройден;
        где не пройден, лестница не строится — это и есть NO_FINITE_N
ЭТАП 3  C-reactivity уже покрыта этапом 1; отдельно смотрятся две
        детерминированно выбранные BORDERLINE-ячейки
```

## 10. Правила остановки и эскалации

| условие | что происходит |
|---|---|
| популяционный интервал при опорном `N` пересекает `±δ` | источник `UNUSABLE` для эндпоинта; лестница `N` не строится, увеличение `N` не предлагается |
| вердикт `MEASUREMENT_AMBIGUOUS`/`BOTH_AMBIGUOUS` | **никогда** не описывается как «не хватило мощности» |
| бюджет `C` | проверяется не здесь, а при заморозке `C`; тогда же переделывается `C`, а не критерий |
| судьба S6 | см. ниже |

### ПОПРАВКА 9 (P1). S5b сам не разблокирует S6

Прежнее правило ссылалось на «целевую рабочую точку», которой в prereg нет.
Это значит, что после просмотра поверхности можно было бы выбрать удобную
точку и ею решить судьбу S6 — то есть выбрать дизайн по результату. Порядок
обратный:

```
S5B_DOES_NOT_UNBLOCK_S6            True
целевая рабочая точка замораживается по ВНЕШНИМ ограничениям ПОСЛЕ S5b,
и уже её предвычисленная ячейка решает, открыт ли S6
```

## 11. Запрещённые интерпретации — дословно

```
«S5b показал, что нужно N участников»
«при таком δ мощность достаточна»
«measurement ambiguity лечится увеличением выборки»
«STRICT в этой области работает лучше»
«S5b оценил бремя пакета C»
«S5b показал, что пакет C выполним»
«мы выбрали H по результатам симуляции»
«opportunity_rate_per_day теперь известен»
«C сокращается в контрасте»
«S5b разблокировал S6»
```

Последние две — трофеи враждебного чтения: обе были в прежней редакции этого
документа не как запреты, а как утверждения.

## 12. Маршрут

```
prereg  →  hostile review #1  →  поправки (здесь)  →  re-review  →  freeze  →  run
                  сделано            сделано          СЛЕДУЮЩЕЕ
```

Константы машинные и заморожены тридцатью шестью тестами. Прогон не
начинается до повторного чтения этой редакции.
